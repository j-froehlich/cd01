<Page
    x:Class="ClickDummy.Shared.xOldX.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClickDummy"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    xmlns:toolkit="using:Uno.UI.Toolkit">

    <Page.TopAppBar>
        <AppBar xmlns:src="using:ClickDummy.Shared.Views.Shared">
            <src:AppBar />
        </AppBar>
    </Page.TopAppBar>

    <Grid toolkit:VisibleBoundsPadding.PaddingMask="Top" Margin="24,24,24,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <CommandBar Grid.Row="0" Content="MainPage" />

        <StackPanel Grid.Row="1">
            <TextBlock Text="Main Content" />
            <Button Content="Next Page" Click="GotoNextPage" />
        </StackPanel>

        <StackPanel Grid.Row="2">
            <TextBlock Text="{Binding Value, ElementName=slider}" Margin="24" FontSize="30" />
            <Slider x:Name="slider"/>
        </StackPanel>
    </Grid>
</Page>
