<UserControl
    x:Class="ClickDummy.Shared.Views.Shared.StatusBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ClickDummy.Shared.Views.Shared"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	  xmlns:toolkit="using:Uno.UI.Toolkit"
	  xmlns:material="using:Uno.Material.Controls"
    mc:Ignorable="d"
    d:DesignHeight="200"
    d:DesignWidth="800">

    <StackPanel Background="{StaticResource MaterialSurfaceBrush}" Margin="8" Orientation="Horizontal">
        <TextBlock Text="Item" Foreground="{StaticResource MaterialOnBackgroundBrush}" />
        <material:Divider Style="{StaticResource MaterialDividerStyle}" />
        <TextBlock Text="Item" Foreground="{StaticResource MaterialOnBackgroundBrush}" />
    </StackPanel>
</UserControl>
